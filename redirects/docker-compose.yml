# GENERATED by ./generate.py
version: '3.7'
services:
  app:
    build: .
    image: ${IMAGE:-registry.sikahq.com/www/www-other/redirects}
    labels:
      traefik.frontend.rule: Host:trainera.cz,www.trainera.cz,kubernetes.is,www.kubernetes.is,kubernetes.sk,www.kubernetes.sk,ondrejsika.cz,www.ondrejsika.cz,ondrejsika.ch,www.ondrejsika.ch,kubernetes.lu,www.kubernetes.lu
      traefik.enable: 'true'
    networks:
        - traefik

networks:
  traefik:
    external:
      name: traefik
